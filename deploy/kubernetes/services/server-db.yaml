apiVersion: v1
kind: Service
metadata:
  name: server-db
  namespace: prod
  labels:
    app: server-db
spec:
  type: ClusterIP
  selector:
    app: server-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
