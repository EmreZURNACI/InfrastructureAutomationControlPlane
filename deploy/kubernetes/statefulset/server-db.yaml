apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: server-db
  namespace: prod
  labels:
    app: server-db
spec:
  serviceName: server-db
  replicas: 3
  selector:
    matchLabels:
      app: server-db
  template:
    metadata:
      labels:
        app: server-db
    spec:
      containers:
        - name: server-db
          image: postgres:15
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/15/main
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: server
                  key: POSTGRES_USER
            - name: POSTGRES_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: server
                  key: POSTGRES_DATABASE
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: server
                  key: POSTGRES_PASSWORD
  volumeClaimTemplates:
    - metadata:
        name: data
        namespace: prod
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
